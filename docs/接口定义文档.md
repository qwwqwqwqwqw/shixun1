# 接口定义文档 — ROS2 话题/服务 + 小程序 TCP 协议

> 负责人：B组长、D组员
> 最后更新：2025-07-12

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

## 一、小程序 ↔ 服务端 TCP Socket 协议

### 连接方式

| 属性 | 值 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|
| 协议 | TCP Socket（非 WebSocket） |
| 服务地址 | 小车 IP（如 `10.112.253.188`） |
| 端口 | `9090` |
| 编码 | UTF-8，每条消息以 `\n` 分隔（JSONL 格式） |
| 小程序 API | `wx.createTCPSocket()` |

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

### 1. 小程序 → 服务端（请求）

#### 1.1 教室导航

```json
{"type":"navigate", "room":"101"}
```

| 字段 | 类型 | 说明 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|
| type | string | 固定 `"navigate"` |
| room | string | 目标教室号 |

#### 1.2 摇杆控制

```json
{"type":"joystick", "vx":0.3, "vy":0.0, "wz":0.8}
```

| 字段 | 类型 | 说明 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|
| type | string | 固定 `"joystick"` |
| vx | float | 线速度 X（前进 0.1~0.5 m/s） |
| vy | float | 线速度 Y（麦轮平移，通常 0） |
| wz | float | 角速度（左转正，右转负，0.5~1.5 rad/s） |

**停止发送 `"vx":0,"vy":0,"wz":0`**。

#### 1.3 人脸识别模式

```json
{"type":"face_mode", "action":"start"}
```

| 字段 | 类型 | 说明 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|
| type | string | 固定 `"face_mode"` |
| action | string | `"start"` 或 `"stop"` |

#### 1.4 心跳

```json
{"type":"ping"}
```

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

### 2. 服务端 → 小程序（响应/推送）

#### 2.1 指令确认

```json
{"type":"ack", "command":"navigate", "room":"101", "status":"accepted"}
```

#### 2.2 导航状态推送

```json
{"type":"nav_status", "message":"开始导航到 101（来源: manual）"}
```

#### 2.3 到达确认推送

```json
{"type":"arrival", "message":true}
```

#### 2.4 心跳响应

```json
{"type":"pong", "message":"ok"}
```

### 1. `/command_room` — 手动教室号指令

| 属性 | 值 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|
| 类型 | `std_msgs/String` |
| 发布者 | `aiserver_node`（D组员） |
| 订阅者 | `guide_node`（B组长） |
| 用途 | 小程序/Web 输入教室号，触发手动导航模式 |

**消息示例：**
```json
{ "data": "101" }
```

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

### 2. `/face_room` — 人脸识别房间号

| 属性 | 值 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|
| 类型 | `std_msgs/String` |
| 发布者 | `face_recognizer`（C组员） |
| 订阅者 | `guide_node`（B组长） |
| 用途 | 人脸识别成功后，发布映射的房间号，触发人脸导航模式 |

**消息示例：**
```json
{ "data": "102" }
```

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

### 3. `/navigation_status` — 导航状态反馈

| 属性 | 值 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|
| 类型 | `std_msgs/String` |
| 发布者 | `guide_node`（B组长） |
| 订阅者 | `aiserver_node`（D组员）、`arrival_fusion`（B组长） |
| 用途 | 实时反馈导航状态给前端和融合节点 |

**状态字符串约定：**
| 状态字符串 | 含义 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|
| `"开始导航到 {room}（来源: {source}）"` | 导航已启动 |
| `"导航中..."` | 正在移动 |
| `"到达 {room}"` | 导航到达目标位置 |
| `"导航失败: {reason}"` | 导航异常 |

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

### 4. `/doorplate_result` — 门牌识别结果

| 属性 | 值 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|
| 类型 | `std_msgs/String` |
| 发布者 | `doorplate_detector`（C组员） |
| 订阅者 | `arrival_fusion`（B组长） |
| 用途 | 门牌识别节点输出检测到的门牌号文本 |

**消息示例：**
```json
{ "data": "101" }
```

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

### 5. `/arrival_confirmed` — 到达确认

| 属性 | 值 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|
| 类型 | `std_msgs/Bool` |
| 发布者 | `arrival_fusion`（B组长） |
| 订阅者 | `aiserver_node`（D组员） |
| 用途 | 融合导航到达 + 门牌匹配后的最终确认信号 |

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | `icar_bringup` 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

## 服务（Services）

### 1. `/navigate_room` — 导航房间服务（可选扩展）

| 属性 | 值 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|
| 类型 | `guide_pkg/srv/NavigateRoom` |
| 服务端 | `guide_node`（B组长） |
| 服务客户端 | `aiserver_node`（D组员） |

**请求（Request）：**
```
string room_number    # 目标教室号
string source         # "manual" 或 "face"
```

**响应（Response）：**
```
bool success          # 是否成功发起导航
string message        # 描述信息
```

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

## 数据流图

```
小程序 ──TCP:9090──> aiserver_node
                      │
                      ├──/command_room──> guide_node ──> Nav2 ──> 小车移动
                      │                       │
                      ├──/cmd_vel──────────────────────> 底盘驱动 ──> 小车移动（摇杆）
                      │                       │
                      │                       ├──/navigation_status──> aiserver_node ──> 小程序
                      │                       └──/navigation_status──> arrival_fusion
                      │                                                    ▲
摄像头 ──> face_recognizer ──/face_room──> guide_node                      │
                                                                          │
摄像头 ──> doorplate_detector ──/doorplate_result────────────────────────►┘
                                                                          │
                          arrival_fusion ──/arrival_confirmed──> aiserver_node ──> 小程序
```

## 坐标系

| 坐标系 | 说明 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|
| `map` | 全局地图坐标系（Nav2 导航目标使用） |
| `odom` | 里程计坐标系 |
| `base_link` | 机器人本体坐标系 |
| `camera_link` | 摄像头坐标系 |

### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---

## QoS 策略

| 话题 | QoS | 说明 |
|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

-----|### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---### 6. `/cmd_vel` — 摇杆速度控制

| 属性 | 值 |
|------|-----|
| 类型 | `geometry_msgs/Twist` |
| 发布者 | `aiserver_node`（D组员，接收小程序摇杆指令） |
| 订阅者 | icar_bringup 底盘驱动节点 |
| 用途 | 小程序摇杆控制小车运动 |

---|
| `/command_room` | RELIABLE, KEEP_LAST(10) | 指令不可丢失 |
| `/face_room` | RELIABLE, KEEP_LAST(10) | 指令不可丢失 |
| `/navigation_status` | RELIABLE, KEEP_LAST(10) | 状态实时反馈 |
| `/doorplate_result` | BEST_EFFORT, KEEP_LAST(1) | 视觉结果允许丢帧 |
| `/arrival_confirmed` | RELIABLE, KEEP_LAST(1) | 确认信号不可丢失 |
| `/cmd_vel` | BEST_EFFORT, KEEP_LAST(1) | 摇杆高频更新，允许丢帧 |
