<view class="container">
  <view class="header">
    <text class="title">iCar 智能教室导航</text>
    <view class="connection {{connected ? 'online' : 'offline'}}"
          bindtap="reconnect">
      <view class="connection-dot"></view>
      <text>{{connected ? '小车已连接' : '未连接，点击重试'}}</text>
    </view>
  </view>

  <view class="section-title">请选择教室</view>
  <view class="room-list">
    <view wx:for="{{rooms}}" wx:key="*this"
          class="room-item {{item == selectedRoom ? 'active' : ''}}"
          bindtap="selectRoom" data-room="{{item}}">
      {{item}}
    </view>
  </view>

  <view class="section-title">或输入其他教室号</view>
  <input class="room-input"
         value="{{customRoom}}"
         maxlength="32"
         placeholder="例如：201"
         bindinput="inputRoom" />

  <button class="btn-primary" bindtap="startNavigate">开始导航</button>

  <view class="manual-card">
    <view class="section-title manual-title">手动控制</view>
    <text class="manual-tip">按住运动，松开立即停止</text>

    <view class="control-grid">
      <view class="control-btn forward {{manualActive == 'forward' ? 'pressed' : ''}}"
            data-action="forward"
            catchtouchstart="startManualControl"
            catchtouchend="stopManualControl"
            catchtouchcancel="stopManualControl">前进</view>
      <view class="control-btn left {{manualActive == 'left' ? 'pressed' : ''}}"
            data-action="left"
            catchtouchstart="startManualControl"
            catchtouchend="stopManualControl"
            catchtouchcancel="stopManualControl">左转</view>
      <view class="control-btn stop" bindtap="emergencyStop">停止</view>
      <view class="control-btn right {{manualActive == 'right' ? 'pressed' : ''}}"
            data-action="right"
            catchtouchstart="startManualControl"
            catchtouchend="stopManualControl"
            catchtouchcancel="stopManualControl">右转</view>
      <view class="control-btn backward {{manualActive == 'backward' ? 'pressed' : ''}}"
            data-action="backward"
            catchtouchstart="startManualControl"
            catchtouchend="stopManualControl"
            catchtouchcancel="stopManualControl">后退</view>
    </view>
  </view>

  <!-- 第二版再启用人脸识别入口
  <view class="divider">或</view>
  <button class="btn-face" bindtap="startFaceRecognition">人脸识别导航（第二版）</button>
  -->
</view>
