# Python 依赖总表 — 所有模块共用
# 安装: pip3 install -r requirements.txt

# ROS2 Python (通常随 ROS2 安装，此处列出供参考)
# rclpy

# 导航
pyyaml>=6.0

# 视觉识别 — 门牌 (EasyOCR 预训练，无需额外训练)
easyocr>=1.7.0
opencv-python>=4.5.0
numpy>=1.21.0

# 视觉识别 — 人脸
face_recognition>=1.3.0
# face_recognition 依赖 dlib，安装前可能需要: sudo apt install cmake

# 接口服务
websockets>=10.0

# 工具
setuptools>=60.0

# 云平台集成 — MQTT 状态上报告 (E组员)
paho-mqtt>=1.6.0
